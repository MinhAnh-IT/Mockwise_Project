name: mockwise

# Production compose — uses pre-built images from GHCR.
# Infrastructure (MySQL, Redis, Kafka, MinIO) runs separately via Mockwise_db.
#
# Deploy:
#   docker compose -f docker/docker-compose.prod.yml --env-file .env up -d

services:

  # ─── Spring Services (internal only, no ports exposed) ─────────
  iam-service:
    image: ghcr.io/minhanh-it/mockwise_project/iam-service:${IMAGE_TAG:-latest}
    restart: unless-stopped
    env_file: ../.env
    environment:
      SPRING_DATASOURCE_URL: jdbc:mysql://${MYSQL_HOST}:${MYSQL_PORT:-3306}/iam
      MYSQL_USER: ${MYSQL_USER}
      MYSQL_PASSWORD: ${MYSQL_PASSWORD}
      SPRING_DATA_REDIS_HOST: ${REDIS_HOST}
      SPRING_DATA_REDIS_PORT: ${REDIS_PORT:-6379}
      REDIS_PASSWORD: ${REDIS_PASSWORD}
      SPRING_KAFKA_BOOTSTRAP_SERVERS: ${SPRING_KAFKA_BOOTSTRAP_SERVERS}
      JWT_COOKIE_SECURE: "false"
      # IAM calls user-profile-service to materialise the profile after a
      # successful registration. Without this, IAM falls back to localhost:8082
      # inside its own container and registration fails with 502.
      EXTERNAL_SERVICES_PROFILE_SERVICE_URL: http://user-profile-service:8082/api/v1/profiles
    networks: [internal-net]

  user-profile-service:
    image: ghcr.io/minhanh-it/mockwise_project/user-profile-service:${IMAGE_TAG:-latest}
    restart: unless-stopped
    env_file: ../.env
    environment:
      SPRING_DATASOURCE_URL: jdbc:mysql://${MYSQL_HOST}:${MYSQL_PORT:-3306}/user_profile
      MYSQL_USER: ${MYSQL_USER}
      MYSQL_PASSWORD: ${MYSQL_PASSWORD}
      IAM_BASE_URL: http://iam-service:8081/api/v1/iam
    networks: [internal-net]
    depends_on: [iam-service]

  mail-service:
    image: ghcr.io/minhanh-it/mockwise_project/mail-service:${IMAGE_TAG:-latest}
    restart: unless-stopped
    env_file: ../.env
    environment:
      SPRING_KAFKA_BOOTSTRAP_SERVERS: ${SPRING_KAFKA_BOOTSTRAP_SERVERS}
      MAIL_HOST: ${MAIL_HOST:-smtp.gmail.com}
      MAIL_PORT: ${MAIL_PORT:-587}
      MAIL_USERNAME: ${MAIL_USERNAME}
      MAIL_PASSWORD: ${MAIL_PASSWORD}
    networks: [internal-net]

  # ─── Question Bank Service (internal only) ─────────────────────
  question-bank-service:
    image: ghcr.io/minhanh-it/mockwise_project/question-bank-service:${IMAGE_TAG:-latest}
    restart: unless-stopped
    env_file: ../.env
    environment:
      # Force prod so application-local.yml's hard-coded dev DB url
      # is not loaded on top of the env vars below.
      SPRING_PROFILES_ACTIVE: prod
      SPRING_DATASOURCE_URL: jdbc:postgresql://${POSTGRES_HOST}:${POSTGRES_PORT:-5432}/question_bank
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      # Sync TTS call after admin creates / updates a behavioral or core question.
      TTS_STT_SERVICE_URL: http://tts-stt-service:8086
      INTERNAL_API_KEY: ${INTERNAL_API_KEY}
    networks: [internal-net]
    depends_on: [tts-stt-service]

  # ─── Storage Service (internal only) ───────────────────────────
  storage-service:
    image: ghcr.io/minhanh-it/mockwise_project/storage-service:${IMAGE_TAG:-latest}
    restart: unless-stopped
    env_file: ../.env
    environment:
      # Force prod so application-local.yml's hard-coded dev values
      # (internal.auth.api-key=dev-internal-key-change-me) are not loaded.
      SPRING_PROFILES_ACTIVE: prod
      SPRING_DATASOURCE_URL: jdbc:postgresql://${POSTGRES_HOST}:${POSTGRES_PORT:-5432}/storage
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      MINIO_ENDPOINT: ${MINIO_ENDPOINT}
      MINIO_PUBLIC_ENDPOINT: ${MINIO_PUBLIC_ENDPOINT:-${MINIO_ENDPOINT}}
      MINIO_PROXY_PUBLIC_BASE_URL: ${MINIO_PROXY_PUBLIC_BASE_URL:-}
      MINIO_ACCESS_KEY: ${MINIO_ACCESS_KEY}
      MINIO_SECRET_KEY: ${MINIO_SECRET_KEY}
      INTERNAL_API_KEY: ${INTERNAL_API_KEY}
    networks: [internal-net]

  # ─── TTS-STT Service (internal only) ──────────────────────────
  tts-stt-service:
    image: ghcr.io/minhanh-it/mockwise_project/tts-stt-service:${IMAGE_TAG:-latest}
    restart: unless-stopped
    env_file: ../.env
    environment:
      # Force prod so application-local.yml's hard-coded dev values
      # (storage-client.base-url, ffmpeg path, scoped api-keys) are not loaded.
      SPRING_PROFILES_ACTIVE: prod
      SPRING_DATASOURCE_URL: jdbc:mysql://${MYSQL_HOST}:${MYSQL_PORT:-3306}/tts_stt?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
      MYSQL_USER: ${MYSQL_USER}
      MYSQL_PASSWORD: ${MYSQL_PASSWORD}
      SPRING_DATA_REDIS_HOST: ${REDIS_HOST}
      SPRING_DATA_REDIS_PORT: ${REDIS_PORT:-6379}
      REDIS_PASSWORD: ${REDIS_PASSWORD}
      SPRING_KAFKA_BOOTSTRAP_SERVERS: ${SPRING_KAFKA_BOOTSTRAP_SERVERS}
      INTERNAL_API_KEY: ${INTERNAL_API_KEY}
      STORAGE_SERVICE_BASE_URL: http://storage-service:8085
      STORAGE_INTERNAL_API_KEY: ${INTERNAL_API_KEY}
      ELEVENLABS_API_KEY: ${ELEVENLABS_API_KEY}
      ELEVENLABS_BASE_URL: ${ELEVENLABS_BASE_URL:-https://api.elevenlabs.io}
      # Vietnamese-first defaults. The default voice (Rachel) is English-trained; for
      # natural Vietnamese pronunciation pick a Vietnamese-native voice from
      # https://elevenlabs.io/app/voice-library?language=vi and override via
      # ELEVENLABS_DEFAULT_VOICE_ID in .env.
      ELEVENLABS_DEFAULT_VOICE_ID: ${ELEVENLABS_DEFAULT_VOICE_ID:-21m00Tcm4TlvDq8ikWAM}
      # eleven_turbo_v2_5 honours language_code (multilingual_v2 ignores it). Combined
      # with ELEVENLABS_DEFAULT_LANGUAGE_CODE=vi this stops the model from auto-detecting
      # short Vietnamese sentences as Indonesian / Thai.
      ELEVENLABS_TTS_MODEL: ${ELEVENLABS_TTS_MODEL:-eleven_turbo_v2_5}
      ELEVENLABS_DEFAULT_LANGUAGE_CODE: ${ELEVENLABS_DEFAULT_LANGUAGE_CODE:-vi}
      ELEVENLABS_STT_MODEL: ${ELEVENLABS_STT_MODEL:-scribe_v1}
      ELEVENLABS_TIMEOUT_TTS_MS: ${ELEVENLABS_TIMEOUT_TTS_MS:-30000}
      ELEVENLABS_TIMEOUT_STT_MS: ${ELEVENLABS_TIMEOUT_STT_MS:-600000}
      KAFKA_TOPIC_ANSWER_SUBMITTED: ${KAFKA_TOPIC_ANSWER_SUBMITTED:-answer-submitted}
      KAFKA_TOPIC_TRANSCRIPT_READY: ${KAFKA_TOPIC_TRANSCRIPT_READY:-transcript-ready}
      KAFKA_TOPIC_TRANSCRIPT_FAILED: ${KAFKA_TOPIC_TRANSCRIPT_FAILED:-transcript-failed}
      FFMPEG_BIN: /usr/bin/ffmpeg
      FFMPEG_TMP_DIR: /tmp/tts-stt
    networks: [internal-net]
    depends_on: [storage-service]

  # ─── Interview Service (internal only) ────────────────────────
  # Orchestrates the candidate's session: pins questions, runs the adaptive
  # planner on per-answer evaluations, and finalises with a cross-question
  # AI overall review. Uses the dedicated mockwise_interview Postgres DB.
  interview-service:
    image: ghcr.io/minhanh-it/mockwise_project/interview-service:${IMAGE_TAG:-latest}
    restart: unless-stopped
    env_file: ../.env
    environment:
      SPRING_PROFILES_ACTIVE: prod
      SPRING_DATASOURCE_URL: jdbc:postgresql://${POSTGRES_HOST}:${POSTGRES_PORT:-5432}/mockwise_interview
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      SPRING_KAFKA_BOOTSTRAP_SERVERS: ${SPRING_KAFKA_BOOTSTRAP_SERVERS}
      INTERNAL_API_KEY: ${INTERNAL_API_KEY}
      # Service-to-service Feign URLs (resolved on internal-net)
      QUESTION_BANK_BASE_URL: http://question-bank-service:8084/api/v1/question-bank
      STORAGE_BASE_URL: http://storage-service:8085/api/v1/storage
      USER_PROFILE_BASE_URL: http://user-profile-service:8082/api/v1/profiles
      TTS_STT_BASE_URL: http://tts-stt-service:8086/api/v1/tts-stt
      AI_BASE_URL: http://ai-service:8000
      AI_SERVICE_API_KEY: ${AI_SERVICE_API_KEY}
      # Kafka topic names. Defaults match what the AI service & tts-stt expect.
      KAFKA_TOPIC_TRANSCRIPT_READY: ${KAFKA_TOPIC_TRANSCRIPT_READY:-transcript-ready}
      KAFKA_TOPIC_TRANSCRIPT_FAILED: ${KAFKA_TOPIC_TRANSCRIPT_FAILED:-transcript-failed}
      KAFKA_TOPIC_EVALUATION_COMPLETED: ${KAFKA_TOPIC_EVALUATION_COMPLETED:-evaluation-completed}
      KAFKA_TOPIC_EVALUATION_FAILED: ${KAFKA_TOPIC_EVALUATION_FAILED:-evaluation-failed}
      KAFKA_TOPIC_SESSION_EVAL_COMPLETED: ${KAFKA_TOPIC_SESSION_EVAL_COMPLETED:-session-evaluation-completed}
      KAFKA_TOPIC_SESSION_EVAL_FAILED: ${KAFKA_TOPIC_SESSION_EVAL_FAILED:-session-evaluation-failed}
    networks: [internal-net]
    depends_on: [question-bank-service, storage-service, tts-stt-service, user-profile-service, ai-service]

  # ─── Practice Service (internal only) ─────────────────────────
  # LeetCode-style practice: browses question-bank's coding catalog (Feign),
  # dispatches Run/Submit to judge-service over Kafka tagged origin=PRACTICE,
  # and owns per-user submission history + status + stats in its own
  # `practice` Postgres DB. Consumes submission-judged with its own group id.
  practice-service:
    image: ghcr.io/minhanh-it/mockwise_project/practice-service:${IMAGE_TAG:-latest}
    restart: unless-stopped
    env_file: ../.env
    environment:
      # Force prod so application-local.yml's localhost defaults are not loaded.
      SPRING_PROFILES_ACTIVE: prod
      SPRING_DATASOURCE_URL: jdbc:postgresql://${POSTGRES_HOST}:${POSTGRES_PORT:-5432}/practice
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      SPRING_KAFKA_BOOTSTRAP_SERVERS: ${SPRING_KAFKA_BOOTSTRAP_SERVERS}
      INTERNAL_API_KEY: ${INTERNAL_API_KEY}
      QUESTION_BANK_BASE_URL: http://question-bank-service:8084/api/v1/question-bank
    networks: [internal-net]
    depends_on: [question-bank-service, judge-service]

  # ─── Judge Service (internal only) ────────────────────────────
  judge-service:
    image: ghcr.io/minhanh-it/mockwise_project/judge-service:${IMAGE_TAG:-latest}
    restart: unless-stopped
    env_file: ../.env
    environment:
      PORT: 8083
      DB_HOST: ${MYSQL_HOST}
      DB_PORT: ${MYSQL_PORT:-3306}
      DB_NAME: judge
      DB_USERNAME: ${MYSQL_USER}
      DB_PASSWORD: ${MYSQL_PASSWORD}
      KAFKA_BOOTSTRAP_SERVERS: ${SPRING_KAFKA_BOOTSTRAP_SERVERS}
      JUDGE0_BASE_URL: ${JUDGE0_BASE_URL}
      JUDGE0_AUTH_TOKEN: ${JUDGE0_AUTH_TOKEN}
      JUDGE_CALLBACK_BASE_URL: ${JUDGE_CALLBACK_BASE_URL}
    # Bound the json-file log. An unrotated judge-service log once grew to
    # 167 GB (tight Kafka-reconnect retry loop) and filled the host disk,
    # crashing Kafka + Postgres. Keep at most 5×50 MB per container.
    logging:
      driver: json-file
      options: { max-size: "50m", max-file: "5" }
    networks: [internal-net, mockwise-judge-net]

  # ─── API Gateway (bridge: public ↔ internal) ───────────────────
  api-gateway:
    image: ghcr.io/minhanh-it/mockwise_project/api-gateway:${IMAGE_TAG:-latest}
    restart: unless-stopped
    env_file: ../.env
    environment:
      PORT: 8888
      IAM_SERVICE_URL: http://iam-service:8081
      USER_PROFILE_SERVICE_URL: http://user-profile-service:8082
      JUDGE_SERVICE_URL: http://judge-service:8083
      QUESTION_BANK_SERVICE_URL: http://question-bank-service:8084
      STORAGE_SERVICE_URL: http://storage-service:8085
      TTS_STT_SERVICE_URL: http://tts-stt-service:8086
      INTERVIEW_SERVICE_URL: http://interview-service:8088
      PRACTICE_SERVICE_URL: http://practice-service:8089
      AI_SERVICE_URL: http://ai-service:8000
      CORS_ORIGIN: ${CORS_ORIGIN:-*}
    # mockwise-judge-net lets the monitoring probe reach judge0-server:2358
    # (same network judge-service uses); the other two carry normal traffic.
    networks: [public-net, internal-net, mockwise-judge-net]
    depends_on: [iam-service, user-profile-service, judge-service, question-bank-service, storage-service, tts-stt-service, interview-service, practice-service]

  # ─── AI Service (internal only) ────────────────────────────────
  # Combined evaluation graph + question selector RAG. Selector half is
  # gated on DATABASE_URL — if Postgres is reachable the lifespan starts
  # the asyncpg pool and Kafka consumer; otherwise the selector endpoints
  # return 503 and only /evaluate, /generate-testcases, /leetcode/fetch work.
  ai-service:
    image: ghcr.io/minhanh-it/mockwise_project/ai-service:${IMAGE_TAG:-latest}
    restart: unless-stopped
    env_file: ../.env
    environment:
      GOOGLE_API_KEY: ${GOOGLE_API_KEY}
      MODEL_NAME: ${MODEL_NAME:-gemini-flash-latest}
      EMBEDDING_MODEL: ${EMBEDDING_MODEL:-gemini-embedding-001}
      EMBEDDING_DIM: ${EMBEDDING_DIM:-768}
      MAX_RETRIES: ${MAX_RETRIES:-2}
      EVALUATOR_VERSION: ${EVALUATOR_VERSION:-evaluator-v1.0}
      SELECTOR_VERSION: ${SELECTOR_VERSION:-selector-v1.0}
      SERVICE_API_KEY: ${AI_SERVICE_API_KEY}
      DATABASE_URL: postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@${POSTGRES_HOST}:${POSTGRES_PORT:-5432}/ai_question_selector
      KAFKA_BOOTSTRAP_SERVERS: ${SPRING_KAFKA_BOOTSTRAP_SERVERS}
      KAFKA_TOPIC_QUESTION_BANK: ${KAFKA_TOPIC_QUESTION_BANK:-question-bank-events}
      KAFKA_CONSUMER_GROUP: ${KAFKA_CONSUMER_GROUP_SELECTOR:-ai-service-selector}
      QUESTION_BANK_BASE_URL: http://question-bank-service:8084
      INTERNAL_API_KEY: ${INTERNAL_API_KEY}
      PORT: 8000
    networks: [internal-net]
    depends_on: [question-bank-service]
    healthcheck:
      test: ["CMD", "python", "-c", "import httpx; httpx.get('http://localhost:8000/health').raise_for_status()"]
      interval: 30s
      timeout: 10s
      start_period: 30s
      retries: 3

  # ─── Frontend (internal, served via nginx) ─────────────────────
  frontend:
    image: ghcr.io/minhanh-it/mockwise_project/frontend:${IMAGE_TAG:-latest}
    restart: unless-stopped
    networks: [internal-net]

  # ─── Swagger UI (internal only) ────────────────────────────────
  swagger-ui:
    image: swaggerapi/swagger-ui:latest
    restart: unless-stopped
    environment:
      BASE_URL: /docs
      URLS: >-
        [{"url":"/api-spec/iam-service.yaml","name":"IAM Service"},
         {"url":"/api-spec/user-profile-service.yaml","name":"User Profile Service"},
         {"url":"/api-spec/judge-service.yaml","name":"Judge Service"},
         {"url":"/api-spec/question-bank-service.yaml","name":"Question Bank Service"},
         {"url":"/api-spec/storage-service.yaml","name":"Storage Service"},
         {"url":"/api-spec/tts-stt-service.yaml","name":"TTS-STT Service"},
         {"url":"/api-spec/interview-service.yaml","name":"Interview Service"}]
    networks: [internal-net]

  # ─── Nginx (only service exposed to the internet) ─────────────
  nginx:
    image: nginx:1.25-alpine
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ../nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      - ../nginx/conf.d:/etc/nginx/conf.d:ro
      - ../api-spec:/etc/nginx/api-spec:ro
      # Let's Encrypt certs (managed by certbot on the host).
      # Mount read-only — the cert files are owned by certbot.
      - /etc/letsencrypt:/etc/letsencrypt:ro
      # Webroot for ACME HTTP-01 challenges (initial issuance + renewals).
      - /var/www/certbot:/var/www/certbot:ro
    networks: [public-net, internal-net]
    depends_on: [api-gateway, frontend, swagger-ui]

networks:
  public-net:
    driver: bridge
  internal-net:
    driver: bridge
  mockwise-judge-net:
    external: true
