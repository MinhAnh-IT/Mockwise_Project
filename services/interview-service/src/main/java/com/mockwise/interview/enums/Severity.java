package com.mockwise.interview.enums;

/**
 * Severity of a single weak target. The planner uses HIGH-severity targets
 * first when picking a follow-up.
 */
public enum Severity {
    LOW,
    MED,
    HIGH
}
